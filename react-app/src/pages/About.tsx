import React from 'react';

class About extends React.Component<Record<string, never>, Record<string, never>> {
  state = {};
  render() {
    return (
      <div>
        <h1>About</h1>
      </div>
    );
  }
}

export default About;
